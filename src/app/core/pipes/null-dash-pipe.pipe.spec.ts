import { NullDashPipePipe } from './null-dash-pipe.pipe';

describe('NullDashPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NullDashPipePipe();
    expect(pipe).toBeTruthy();
  });
});
