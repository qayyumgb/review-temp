import { RemoveSpecialCharactersDirective } from './remove-special-characters.directive';

describe('RemoveSpecialCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSpecialCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
